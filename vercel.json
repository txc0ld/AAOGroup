{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && cd web && corepack prepare pnpm@10.28.2 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "cd web && pnpm build",
  "outputDirectory": "web/.next",
  "regions": ["syd1"]
}
