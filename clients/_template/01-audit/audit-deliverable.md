<!--
Placeholder. This file is written by `aao-audit-report-renderer`, which takes the orchestrator output in `audit-pipeline-output.json` plus the client metadata and produces a fully formatted Markdown audit report matching the structure in `sales-assets/03-audit-report-sample.md`.

DO NOT type the report here by hand. The motion is:

  1. Make sure `audit-pipeline-output.json` is populated.
  2. Invoke `aao-audit-report-renderer` with that output + client metadata.
  3. The renderer writes the full report into THIS file, overwriting the stub below.
  4. Operator review (read end-to-end, fix anything wrong, sanity-check ROI).
  5. Export to PDF (browser print or pandoc) for client delivery.
-->

# Operations Audit - {{client_trading_name}}

_Status: not yet rendered. Run `aao-audit-report-renderer` to populate._
